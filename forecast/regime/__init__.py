"""行情模式识别：预测算法的核心——识别并过滤不适合网格的行情。

网格不利模式为「低残差、强趋势」的低震荡路径（网格被单边穿越）。方法分三层，
各层评估口径独立，避免把「识别准」与「交易少」混为一谈：

- labels.py     模式定义：逐锚点残差趋势规则命中视作带噪观测，粘性前向-后向
                平滑成连片的事后标签（真值，训练目标与回测 oracle 参照）；
- classify.py / train.py
                模式识别：LightGBM 二分类 P(不利 | 47 维窗口特征 + symbol_id)，
                AUC / AP 对着事后标签评估；门控阈值 τ 在验证段按真值占比率配平；
- economics.py  经济验证：常开回放下按模式占比分桶的日度 g——模式经济含义的
                主证据（负期望基线下门控回测的 mean_g 会奖励「交易少」）。

门控执行在 strategy/engine（只在净持仓为 0 时读取当拍信号）；统一回测入口为
strategy/backtest.py，可视化为 webviz。产物在 forecast/regime/runs/。
"""
